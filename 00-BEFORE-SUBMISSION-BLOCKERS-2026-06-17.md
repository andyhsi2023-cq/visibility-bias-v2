# Before-submission blockers — visibility-bias-v2 (2026-06-17)

Status: **REAL methods/instrument finding (VAI), honestly repositioned** (H2 inspection null reported as "definitively null" — a strength). Honest tier T3 methods-track (JCSS). The VAI→CIR pillar (β≈+0.111, p=0.002) and the construct-validity battery trace cleanly. BUT a reviewer running the repo will hit the items below. I fixed the one fully-verifiable error; the rest need YOUR original scripts/logs (I do not ship guessed numbers).

## ✅ Fixed by me (verified against `phase-B2/extended_sun_abraham.csv`)
- **9-cohort Sun-Abraham SE: 0.117 → 0.012** (a 10× transcription error; the 0.117 was copied from the *5-cohort* row above, whose 0.117 is correct = CSV 0.1166). With β=+0.016, SE=0.012 gives t≈1.3, p=0.19 — now internally consistent. Fixed in `04_results.md:128` and `06_appendix_deviations.md:45`.

## ⚠️ You must reconcile before submission (I verified these are genuine, but cannot fix without your logs)
1. **Table 2 rows 4–6 do not trace to the archive.** Row 4 "no-city-FE β=+0.089" — no spec-curve row produces +0.089 (the only 0.089 on disk is an unrelated welfare param). Row 5 "no-year-FE +0.184" — the only +0.184 on disk is the *above-median fiscal-revenue heterogeneity slice* (Appendix C.6), not a no-year-FE spec → likely a **labeling error**. Row 6 "IV (VAI_lag1) +0.142" — not archived. → Re-run these 3 specs and deposit the outputs, or correct the table. (Rows 1–3 are clean: `phase-E/cir_replication.csv`.) **This is the single highest-priority item — 4 of 6 rows of your headline robustness table.**
2. **"City-FE strengthens" narrative depends on Row 4.** Table 2 implies city+year-FE (+0.113) > no-city-FE (+0.089), i.e. strengthening — but the archived spec-curve goes the *opposite* way (removing city FE raises the coef to ≈+0.226). The "strengthening" claim rests on the unverifiable Row 4; confirm or drop it.
3. **Spec-curve count: manuscript "24 permutations" vs archive 96.** `04_results.md:37` / `05_welfare_and_policy.md:10` / `06_appendix:120` say "24 permutations, median +0.107, 24/24 positive, 22/24 sig". The archive (`spec_curve_results.csv`) has **96 VAI-CIR specs, 96/96 positive, 95.8% sig, median 0.124** — i.e. *stronger* than claimed, but the 24-row pruned file is absent. → Deposit the 24-spec pruned output (with the pruning rule), or report the 96.
4. **Total-investment null sign — confirm the spec.** `04_results.md:30` reports β=−0.008 (p=0.71) under city+year FE on the n≈2,751 sample. The `vai_substitution_results.csv` shows +0.191 (p=0.226) — but on a *different* sample (n=2,358) and construction, so this is likely a different spec, not a contradiction. → Point the manuscript's −0.008 at its source output file (confirm it traces) so a replicator doesn't land on the +0.191 row and see a sign flip.
5. **Replication archive split across two projects.** The spec-curve and consequence-chain CSVs physically live in the **sibling `visibility-bias-upgrade/03-analysis/`**, not in `visibility-bias-v2/`; and `README`/Online-Appendix E cites `03-analysis/phase-A/specification_curve_v2.py`, which does **not exist** in v2 (no `phase-A/`). → Mirror the sibling artifacts into the v2 Zenodo record and fix the README paths.

## Recommended framing softenings (not blockers; reduce hostile-reviewer surface)
- Abstract `00_abstract.md:3`: "VAI predicts … without affecting total investment" → "VAI is positively associated with … with no detectable association with total investment" (it's an association under FE, not a causal effect).
- `05_welfare_and_policy.md`: welfare "the compositional distortion **implies** ¥1–15B" → "a calibration anchored in the MOHURD share a*=0.538 and a benchmark a^SO∈[0.40,0.50] **yields** ¥1–15B as an order-of-magnitude illustration" (the ¥1–15B is a MOHURD-anchored calibration, not a VAI-instrument result; same for the abstract's "first empirical estimates").
- §6.2 "officials **systematically allocate** text-rhetorical attention" → "GWRs **exhibit a within-city association** … consistent with (not uniquely identifying) the cadre-attention mechanism".
